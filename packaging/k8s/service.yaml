apiVersion: v1
kind: Service
metadata:
  name: geometry-spine
  namespace: geometry-system
spec:
  selector:
    app: geometry-spine
  ports:
  - name: mcp-http
    port: 3030
    targetPort: 3030
  type: LoadBalancer
